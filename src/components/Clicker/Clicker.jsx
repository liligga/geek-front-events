function Clicker() {
    const buttonHandler = (event) => {

    } 

    const clickHandler = function(event) {
    }

    return (
        <>
            <button onClick={}>Нажми на меня</button><br/>
            <a href="https://google.com" onClick={}>Гугл</a>
            <div className="box border" onClick={}>
                BOX
            </div>
        </>
    )
}

export default Clicker